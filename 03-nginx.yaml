- name: install nginx
  hosts: frontend
  become: yes
  tasks:
  - name: install nginx server
    ansible.builtin.package:
      name: nginx
      state: present


  - name: install nginx server
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes    

